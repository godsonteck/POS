{
  "name": "dual-shop-retail-pos",
  "private": true,
  "version": "0.1.0",
  "description": "Offline-first retail POS with online synchronization for independent shop installations.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -b"
  },
  "engines": { "node": ">=20" },
  "packageManager": "npm@11"
}
